import 'package:flutter/material.dart';

class Paeg3 extends StatelessWidget {
  const Paeg3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blueGrey,
      appBar: AppBar(
        title: Text("3"),
      ),
    );
  }
}
