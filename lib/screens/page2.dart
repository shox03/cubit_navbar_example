import 'package:flutter/material.dart';

class Paeg2 extends StatelessWidget {
  const Paeg2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blueGrey,
      appBar: AppBar(
        title: Text("2"),
      ),
    );
  }
}
